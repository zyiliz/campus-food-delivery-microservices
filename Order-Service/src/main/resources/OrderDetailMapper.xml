<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.OrderDetailMapper">

    <insert id="batchInsertByAnnotation">
        INSERT INTO order_detail (order_id, name, dish_id, image, price, quantity)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.name}, #{item.dishId}, #{item.image}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>

    <select id="getTop5CoOccurrenceDishes" resultType="com.example.dto.CoPurchasedDishDTO">
        SELECT
        od_other.dish_id,
        od_other.name,
        COUNT(od_other.id) AS frequency
        FROM tb_order_detail od_base
        INNER JOIN tb_order o ON od_base.order_id = o.id
        INNER JOIN tb_order_detail od_other ON od_base.order_id = od_other.order_id
        WHERE
        -- 匹配包含任意一个目标 dishId 的订单
        od_base.dish_id IN
        <foreach collection="targetDishIds" item="dishId" open="(" separator="," close=")">
            #{dishId}
        </foreach>
        -- 仅统计已完成订单 (status=5)
        AND o.status = 5
        -- 排除传入的目标 dishId 本身
        AND od_other.dish_id NOT IN
        <foreach collection="targetDishIds" item="dishId" open="(" separator="," close=")">
            #{dishId}
        </foreach>
        GROUP BY od_other.dish_id, od_other.name
        ORDER BY frequency DESC
        LIMIT 5
    </select>

</mapper>